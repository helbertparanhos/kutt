{{> header}}
<section id="dashboard" class="dashboard-page">

  <div class="dash-topbar">
    <h1 class="dash-title">Dashboard</h1>
    <div class="dash-actions">
      <a class="button" href="/api/v2/links/export?format=csv" download>
        {{> icons/write}}&nbsp;Export CSV
      </a>
      <a class="button secondary" href="/api/docs" target="_blank">API Docs</a>
    </div>
  </div>

  {{!-- KPI Cards --}}
  <div class="dash-kpis">
    <div class="dash-kpi">
      <span class="kpi-value">{{total_links}}</span>
      <span class="kpi-label">Total Links</span>
    </div>
    <div class="dash-kpi">
      <span class="kpi-value primary">{{total_clicks}}</span>
      <span class="kpi-label">Total Clicks</span>
    </div>
    <div class="dash-kpi">
      <span class="kpi-value">{{avg_clicks}}</span>
      <span class="kpi-label">Avg Clicks / Link</span>
    </div>
    <div class="dash-kpi">
      <span class="kpi-value secondary">{{links_last_30_days}}</span>
      <span class="kpi-label">Links (Last 30 Days)</span>
    </div>
  </div>

  {{!-- Main content: chart + recent --}}
  <div class="dash-grid">

    {{!-- Top links chart + table --}}
    <div class="dash-card dash-card-wide">
      <div class="dash-card-header">
        <h2>Top 10 Links by Clicks</h2>
      </div>
      <div class="dash-chart-wrap">
        <canvas id="topLinksChart"></canvas>
      </div>
      <table class="dash-table">
        <colgroup>
          <col style="width:40px">
          <col style="width:120px">
          <col>
          <col style="width:90px">
          <col style="width:46px">
        </colgroup>
        <thead>
          <tr>
            <th>#</th>
            <th>Slug</th>
            <th>Destination</th>
            <th style="text-align:right">Clicks</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          {{#each top_links}}
          <tr>
            <td class="dash-rank">{{addOne @index}}</td>
            <td><strong>{{address}}</strong></td>
            <td class="dash-target" title="{{target}}">{{target}}</td>
            <td style="text-align:right"><strong>{{visit_count}}</strong></td>
            <td>
              <a class="button small" href="/stats?id={{id}}" title="Stats">{{> icons/chart}}</a>
            </td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    </div>

    {{!-- Recently created --}}
    <div class="dash-card">
      <div class="dash-card-header">
        <h2>Recently Created</h2>
        <a class="nav" href="/">+ New link</a>
      </div>
      <table class="dash-table">
        <colgroup>
          <col>
          <col style="width:70px">
          <col style="width:46px">
        </colgroup>
        <thead>
          <tr>
            <th>Slug</th>
            <th style="text-align:right">Clicks</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          {{#each recently_created}}
          <tr>
            <td>
              <strong>{{address}}</strong>
              <div class="dash-target">{{relative_created_at}}</div>
            </td>
            <td style="text-align:right">{{visit_count}}</td>
            <td>
              <a class="button small" href="/stats?id={{id}}" title="Stats">{{> icons/chart}}</a>
            </td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    </div>

  </div>

</section>
{{> footer}}

{{#extend "scripts"}}
<script src="/libs/chart.min.js"></script>
<script>
(function () {
  var data = {{{top_links_json}}};
  if (!data || !data.length) return;
  var ctx = document.getElementById("topLinksChart");
  if (!ctx) return;
  new Chart(ctx, {
    type: "bar",
    data: {
      labels: data.map(function(d){ return d.label; }),
      datasets: [{
        label: "Clicks",
        data: data.map(function(d){ return d.value; }),
        backgroundColor: [
          "hsla(207,90%,54%,0.75)","hsla(262,47%,55%,0.75)",
          "hsla(144,50%,50%,0.75)","hsla(35,90%,54%,0.75)",
          "hsla(0,80%,55%,0.75)","hsla(180,70%,45%,0.75)",
          "hsla(300,50%,55%,0.75)","hsla(60,80%,48%,0.75)"
        ],
        borderRadius: 6,
        borderWidth: 0,
      }]
    },
    options: {
      responsive: true,
      maintainAspectRatio: true,
      plugins: { legend: { display: false } },
      scales: {
        y: { beginAtZero: true, grid: { color: "hsla(200,20%,85%,0.5)" }, ticks: { font: { size: 12 } } },
        x: { grid: { display: false }, ticks: { font: { size: 12 } } }
      }
    }
  });
}());
</script>
{{/extend}}
